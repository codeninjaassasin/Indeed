# Runtime state — machine-specific, not for version control
seen_jobs.json
safe_proxies.json
indeed_session.json

# Contains live webhook URL — keep the .example, not the real one
.env

# Config has user-specific filters — keep example, not the real one
config.json

# Certs / CA bundles
certs/

# VPN private keys are sensitive
vpn/*.conf
vpn/.env

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Wireproxy binary (platform-specific)
wireproxy

# macOS
.DS_Store
